const storageKey='cabine-pro-theme';
function applyTheme(theme){document.documentElement.setAttribute('data-theme',theme);localStorage.setItem(storageKey,theme);document.querySelectorAll('[data-theme-toggle]').forEach(btn=>{btn.textContent=theme==='dark'?'☀️ Light':'🌙 Dark';});}
function initTheme(){const saved=localStorage.getItem(storageKey)||'light';applyTheme(saved);document.querySelectorAll('[data-theme-toggle]').forEach(btn=>btn.addEventListener('click',()=>applyTheme(document.documentElement.getAttribute('data-theme')==='dark'?'light':'dark')));}
function toast(msg){let el=document.querySelector('.toast');if(!el){el=document.createElement('div');el.className='toast';document.body.appendChild(el);}el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1700)}
function copyText(id){const el=document.getElementById(id);if(!el)return; navigator.clipboard?.writeText(el.textContent.trim()); toast('Texto copiado.');}
function initCopy(){document.querySelectorAll('[data-copy]').forEach(btn=>btn.addEventListener('click',()=>copyText(btn.dataset.copy)));}
function initLogin(){const form=document.querySelector('[data-login-form]'); if(form){form.addEventListener('submit',e=>{e.preventDefault();toast('Login demo realizado.');setTimeout(()=>{window.location.href='app.html'},450);});}}
function initCreateBriefing(){const form=document.querySelector('[data-create-briefing]');if(form){form.addEventListener('submit',e=>{e.preventDefault();toast('Briefing gerado com 96% de confiança.');setTimeout(()=>{window.location.href='briefing.html'},450);});}}
function initFontControls(){const tele=document.querySelector('[data-teleprompter]');if(!tele)return;let size=24;document.querySelector('[data-font-plus]')?.addEventListener('click',()=>{size=Math.min(38,size+2);tele.style.setProperty('--tp-size',size+'px')});document.querySelector('[data-font-minus]')?.addEventListener('click',()=>{size=Math.max(18,size-2);tele.style.setProperty('--tp-size',size+'px')});document.querySelector('[data-fullscreen]')?.addEventListener('click',()=>{if(tele.requestFullscreen)tele.requestFullscreen();else toast('Tela cheia indisponível neste navegador.')});}
function initSafeMode(){const btn=document.querySelector('[data-safe-toggle]');if(!btn)return;btn.addEventListener('click',()=>{const on=btn.dataset.state!=='off';btn.dataset.state=on?'off':'on';btn.textContent=on?'Modo Seguro: OFF':'Modo Seguro: ON';document.querySelectorAll('[data-risk="verify"]').forEach(el=>el.style.display=on?'block':'none');toast(on?'Modo Seguro desativado.':'Modo Seguro ativado.');});}
function initSourceFilters(){const buttons=document.querySelectorAll('[data-source-filter]');if(!buttons.length)return;buttons.forEach(btn=>btn.addEventListener('click',()=>{const filter=btn.dataset.sourceFilter;buttons.forEach(b=>b.classList.remove('dark'));btn.classList.add('dark');document.querySelectorAll('[data-source-level]').forEach(row=>{row.style.display=filter==='all'||row.dataset.sourceLevel===filter?'grid':'none'});}));}
function init(){initTheme();initCopy();initLogin();initCreateBriefing();initFontControls();initSafeMode();initSourceFilters();}
document.addEventListener('DOMContentLoaded',init);